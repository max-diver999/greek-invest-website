/**
 * Пороги AI-каденции для greek-invest-website (английский корпус).
 *
 * Движок `scripts/lib/ai-cadence.mjs` и CLI `scripts/cadence-check.mjs` общие с
 * остальными сайтами MORE Group, байт в байт. Разница между сайтами живёт
 * только здесь: язык шаблонов и потолки по коллекциям. Расхождение копий
 * движка ловит `npm run cadence:parity`.
 *
 * КАК МЕНЯТЬ ПОРОГИ. Числа посчитаны командой `node scripts/cadence-check.mjs
 * --calibrate --json` по живому корпусу одним и тем же правилом на всех сайтах:
 *   maxPer1k        = p90 плотности, округлённый до ближайших 0.5, но не ниже 2.5;
 *   maxMetronomePct = p90 метронома, округлённый вверх до кратного 5, в пределах 20..60;
 *   minCv           = p10 CV минус 0.05, округлённый вниз до 0.05, в пределах 0.25..0.35.
 * Профиль раздела заводится только там, где в разделе не меньше 8 файлов
 * длиннее его порога слов; числа для него считаются тем же правилом по данным
 * раздела. Порог из головы здесь запрещён: гейт по GEO-баллу, выставленный до
 * замера, кончился тем, что машинные тексты набирали 92, а рукописные 78.
 */

export const LANG = 'en';

/** Замер 22.09.2026, 156 файлов длиннее 250 слов, ветка cc/cadence-port. */
export const CALIBRATION = {
  measuredAt: '2026-09-22',
  files: 156,
  corpus: {
    per1k: { p50: 1.79, p75: 2.57, p90: 3.32, max: 6.11 },
    metronomePct: { p50: 17, p75: 22, p90: 27, max: 47 },
    cv: { p10: 0.48, p25: 0.53, p50: 0.59 },
  },
  /** Сумма попаданий по категориям на весь корпус. */
  categories: {
    antithesis: 620,
    superlativeOpener: 31,
    colonHook: 19,
    clerical: 2,
    calque: 2,
    headingHook: 2,
    literary: 0,
    impersonal: 0,
  },
  /**
   * Вывод тот же, что на Пхукете: почти весь почерк даёт одна фигура речи,
   * антитеза («a plan, not panic»): 620 попаданий из 676, около 4 на файл.
   * Канцелярит, пафос и учительский тон, которые ловил прежний гейт
   * (lib/more-content-gate.mjs), дают на весь корпус 2, 0 и 0 попаданий.
   *
   * Корпус здесь чище английского Пхукета по плотности (p90 3.32 против 5.67)
   * и по метроному (p90 27% против 33%), поэтому потолки ниже.
   */
  dominantCategory: 'antithesis',
};

export const CADENCE_THRESHOLDS = {
  // p75 2.57 / p90 3.32 -> 3.5
  maxPer1k: 3.5,
  // метроном p75 22 / p90 27.4 -> 30
  maxMetronomePct: 30,
  // CV p10 0.48 - 0.05 = 0.43 -> 0.40, потолок правила 0.35
  minCv: 0.35,
  maxParallel: 3,
  // Повтор n-граммы шумит на шаблонных карточках проектов: только отчёт.
  maxRepeated: null,
  // Ниже 300 слов плотность на 1000 слов перестаёт быть устойчивой: четыре
  // попадания на 264 слова дают 15/1k и выглядят как провал на ровном месте.
  minWords: 300,
};

/**
 * Профили по коллекциям, каждый по своим квантилям.
 * Без профиля (меньше 8 файлов длиннее порога слов, работает база):
 * developers (4), living-in-greece (3), golden-visa (2), news (2).
 */
export const COLLECTION_PROFILES = {
  // n=97; плотность p75 2.59 / p90 3.30 -> 3.5; метроном p75 20.6 / p90 25.0 -> 25;
  // CV p10 0.48 -> 0.35
  guides: { maxPer1k: 3.5, maxMetronomePct: 25, minCv: 0.35 },
  // n=13; плотность p75 2.38 / p90 3.00 -> 3.0; метроном p75 22.2 / p90 27.0 -> 30;
  // CV p10 0.55 -> 0.35
  compare: { maxPer1k: 3.0, maxMetronomePct: 30, minCv: 0.35 },
  // n=12; плотность p75 2.57 / p90 2.78 -> 3.0; метроном p75 19.0 / p90 24.4 -> 25;
  // CV p10 0.47 -> 0.35
  areas: { maxPer1k: 3.0, maxMetronomePct: 25, minCv: 0.35 },
  // n=9, карточки; плотность p75 2.74 / p90 4.08 -> 4.0; метроном p75 21.7 / p90 23.7 -> 25;
  // CV p10 0.43 -> 0.35
  projects: { maxPer1k: 4.0, maxMetronomePct: 25, minCv: 0.35, minWords: 400, maxParallel: 4 },
  // n=14, карточки; плотность p75 1.74 / p90 1.80 -> 2.0, пол правила 2.5;
  // метроном p75 26.6 / p90 29.3 -> 30; CV p10 0.50 -> 0.35
  'property-for-sale': { maxPer1k: 2.5, maxMetronomePct: 30, minCv: 0.35, minWords: 400, maxParallel: 4 },
};

export function cadenceProfileFor(collection) {
  return { ...CADENCE_THRESHOLDS, ...(COLLECTION_PROFILES[collection] || {}) };
}
