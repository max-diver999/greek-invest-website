---
import BaseLayout from './BaseLayout.astro';
import LeadForm from '../components/LeadForm.astro';
import FaqBlock from '../components/FaqBlock.astro';
import { SITE } from '../data/site';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  author?: string;
  readingTime?: number;
  heroImage?: string;
  heroAlt?: string;
  faq?: { question: string; answer: string }[];
  canonical?: string;
  noindex?: boolean;
  hasInlineFaqBlock?: boolean;
  contextLabel?: string;
}

const {
  title,
  description,
  pubDate,
  updatedDate,
  author = SITE.editorial,
  readingTime = 8,
  heroImage,
  heroAlt,
  faq = [],
  canonical,
  noindex = false,
  hasInlineFaqBlock = false,
  contextLabel = title,
} = Astro.props;

const dateStr = (updatedDate || pubDate).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
const pageUrl = canonical || Astro.url.href;

const articleSchema = {
  '@context': 'https://schema.org',
  '@type': 'Article',
  headline: title,
  description,
  datePublished: pubDate.toISOString(),
  dateModified: (updatedDate || pubDate).toISOString(),
  author: {
    '@type': 'Organization',
    name: author,
    url: `${SITE.url}/about/`,
  },
  publisher: {
    '@type': 'Organization',
    name: SITE.name,
    url: SITE.url,
    logo: { '@type': 'ImageObject', url: `${SITE.url}/favicon-192.png`, width: 192, height: 192 },
  },
  isAccessibleForFree: true,
  inLanguage: 'en',
  mainEntityOfPage: pageUrl,
  ...(heroImage ? { image: heroImage } : {}),
};

const faqSchema = faq.length > 0 ? {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faq.map((item) => ({
    '@type': 'Question',
    name: item.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: item.answer,
    },
  })),
} : null;

const pathParts = Astro.url.pathname.split('/').filter(Boolean);
const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: SITE.url },
    ...(pathParts.length > 1 ? [{
      '@type': 'ListItem',
      position: 2,
      name: pathParts[0].charAt(0).toUpperCase() + pathParts[0].slice(1),
      item: `${SITE.url}/${pathParts[0]}/`,
    }] : []),
    {
      '@type': 'ListItem',
      position: pathParts.length > 1 ? 3 : 2,
      name: title,
      item: pageUrl,
    },
  ],
};

// Single FAQPage owner. The inline <FaqBlock> in MDX emits its own FAQPage, so the
// layout must not emit a second one — 132 pages previously shipped duplicates and
// 7 shipped conflicting question sets.
const schemas = [articleSchema, breadcrumbSchema, ...(faqSchema && !hasInlineFaqBlock ? [faqSchema] : [])];
---
<BaseLayout
  title={title}
  description={description}
  canonical={pageUrl}
  ogImage={heroImage}
  type="article"
  {noindex}
  {schemas}
>
  <article class="site-container py-10 md:py-14">
    <header class="max-w-3xl mb-8">
      <span class="badge-pill mb-4">Research guide</span>
      <h1 class="mb-4 text-3xl md:text-4xl">{title}</h1>
      <p class="text-lg text-[var(--color-muted)] mb-4">{description}</p>
      <p class="text-sm text-[var(--color-muted)]">
        By {author} · Updated {dateStr} · {readingTime} min read
      </p>
    </header>

    {heroImage && (
      <img src={heroImage} alt={heroAlt || `${title} — Greek Invest research illustration`} class="w-full max-h-[420px] object-cover rounded-2xl mb-10 border border-[var(--color-border)]" width="1200" height="420" loading="eager" fetchpriority="high" />
    )}

    <div class="prose mx-auto">
      <slot />
    </div>

    {!hasInlineFaqBlock && faq.length > 0 && (
      <div class="max-w-3xl mx-auto mt-10">
        <FaqBlock items={faq} />
      </div>
    )}

    <div class="max-w-3xl mx-auto mt-12">
      <LeadForm
        title="Get your Greece property shortlist"
        subtitle="Share your budget, region (Athens, Crete, islands, or Peloponnese), and Golden Visa goal. We reply within one business day with matched options and next steps."
        context={contextLabel}
      />
    </div>

    <aside class="max-w-3xl mx-auto mt-10 surface-card p-5 text-sm text-[var(--color-muted)]">
      <strong>Disclaimer:</strong> Greek Invest is an independent editorial resource. We are not a developer, law firm, or licensed Greek broker.
      Enquiries may be referred to licensed Greek lawyers and partner agents. Golden Visa thresholds, taxes, and transfer rules change; verify with your lawyer and AADE before any transaction.
    </aside>
  </article>
</BaseLayout>
